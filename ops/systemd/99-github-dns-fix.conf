# Bypass router IPv6 DNS (fe80::...) that resolves github.com to 127.0.0.1.
# Route all queries through trusted resolvers only (see ops/scripts/fix-dns-github.sh).
[Resolve]
DNS=223.5.5.5 192.168.101.2
FallbackDNS=1.1.1.1 8.8.8.8
Domains=~.
